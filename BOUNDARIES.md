# .github Boundaries

## Role
`.github` provides organization-wide community defaults and workflow scaffolding for OpenSIN-AI.

## Hard rules

- Keep this repo focused on org-wide templates and defaults.
- Do not let this repo become a second source of truth for runtime, product, or docs implementation details.
- Repo-specific authority rules belong in the actual repo that owns the concern.
